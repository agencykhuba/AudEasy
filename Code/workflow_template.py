# SLM Workflow Template
def run_workflow(niche, features):
    print(f'Generating app for {niche} with features: {features}')
if __name__ == '__main__':
    run_workflow('audit app', ['login', 'audit form', 'CAPA'])